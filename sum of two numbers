#include <stdio.h>

int main()
{
    int sum, a, b;

    printf("Enter the value of a: ");
    scanf("%d", &a);

    printf("Enter the value of b: ");
    scanf("%d", &b);

    sum = a + b;

    printf("Sum of %d and %d is %d\n", a, b, sum);

    return 0;
}